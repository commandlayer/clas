{
  "name": "@commandlayer/clas",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "npm run validate:trust-verification && npm run validate:execution",
    "validate": "npm run validate:trust-verification && npm run validate:execution",
    "validate:trust-verification": "node scripts/validate-trust-verification-examples.mjs",
    "validate:execution": "node scripts/validate-execution-receipts.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
